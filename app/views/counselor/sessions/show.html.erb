<h1><%= formatted_date(text_to_date(@url_date)) %></h1>
<table>
  <tr>
    <th>Start Time</th>
    <th>End Time</th>
    <th>Available?</th>
    <th>Student</th>
  </tr>
  <% @todays_sessions.each do |session| %>
    <tr>
      <td><%= sesh.start_time %></td>
      <td><%= sesh.end_time %></td>
      <td><% if sesh.available %>
          Yes
        <% else %>
          No
        <% end %>
      </td>
      <td>
        <% if !(session.available) %>
          <%= User.find(Appointment.find_by_session_id(session.id).student_id).first_name %>
        <% end %>
      </td>
    </tr>
  <% end %>
</table>
